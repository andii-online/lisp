number = { "-"* ~ ASCII_DIGIT+ }
operator = { "+" | "-" | "/" | "*" }
expression = { number | "(" ~ operator ~ " " ~ expression ~ (" " ~ expression)+ ~ ")" }
rispy = { operator ~ " " ~ expression ~ (" " ~ expression)+ }
